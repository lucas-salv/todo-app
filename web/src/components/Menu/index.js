import { Container, MainMenu, SecondaryMenu } from './styles';

export default function Menu({ open }) {
    return (
        <Container open={open}>
            
            <MainMenu>

            </MainMenu>
            <SecondaryMenu>

            </SecondaryMenu>
        </Container>
    )
}