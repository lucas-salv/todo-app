import React from 'react';
import FormLogin from './../components/FormLogin';

const defaultObj = {
    title: 'components/FormLogin',
    component: FormLogin
};

export default defaultObj;

export const Responsive = () => <FormLogin />